# Names only. TODO(secrets-system-prompt): inject via secret manager; the app holds no provider key.
AISEC_GATEWAY_BASE_URL=http://gateway:8080     # existing LLM gateway (see agent-workflow/gateway or your own)
AISEC_MODEL=
AISEC_GATEWAY_API_KEY=                              # only if your gateway requires a client key; TODO(secrets-system-prompt): never a provider key
AISEC_EMBED_MODEL=
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=docs
INGEST_SOURCE_TOKEN=                            # ingest job only. TODO(memory-rag-integrity): read-only, narrowest connector scope
