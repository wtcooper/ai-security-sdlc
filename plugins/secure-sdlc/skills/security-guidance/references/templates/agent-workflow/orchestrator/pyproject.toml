[project]
name = "agent-workflow-orchestrator"
version = "0.1.0"
requires-python = ">=3.12"
# Versions checked on PyPI 2026-08-16. TODO(codeguard:supply-chain): keep a lockfile (uv lock) and hash-pin.
dependencies = [
  "langgraph==1.2.11",
  "langchain==1.3.15",
  "langchain-openai==1.5.1",
  "fastapi==0.141.1",
  "uvicorn==0.52.3",
  "httpx==0.28.1",
  "pydantic==2.13.4",
]
