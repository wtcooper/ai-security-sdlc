# Names only. TODO(secrets-system-prompt): no downstream credentials in the server image; broker per call.
MCP_ISSUER_URL=https://auth.example.com          # your authorization server
MCP_RESOURCE_URL=https://mcp.example.com/mcp     # this server's canonical URL (aud) — TODO(model-gateway-trust): must match TLS terminator
MCP_JWKS_URL=                                     # TODO(codeguard:authentication)
DOWNSTREAM_BASE_URL=                              # the system these tools front
